# dlt_pipelines/.dlt/secrets.toml
# DLT reads this automatically. Credentials here override environment variables.
# For production: use env vars and delete this file.

[sources.sql_database.credentials]
# Oracle is passed programmatically in oracle_to_staging.py
# Leave this file here so DLT doesn't complain about missing config dir.

[destination.postgres.credentials]
# Also set programmatically — warehouse URL comes from WAREHOUSE_PG_URL env var

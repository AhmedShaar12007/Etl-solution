model-paths:
- models
models:
  test_warehouse:
    marts:
      +materialized: incremental
      +on_schema_change: sync_all_columns
      +schema: marts
    staging:
      +materialized: view
      +schema: staging
name: test_warehouse
profile: test_warehouse
version: 1.0.0

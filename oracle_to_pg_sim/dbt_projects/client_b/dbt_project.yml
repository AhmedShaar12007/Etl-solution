name: client_b_warehouse
version: "1.0.0"
profile: client_b_warehouse

model-paths: ["models"]

models:
  client_b_warehouse:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: incremental
      +schema: marts
      +on_schema_change: sync_all_columns